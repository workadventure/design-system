
.pagination {
    @apply m-0 p-0;
    .pagination-item {
        @apply w-3 h-1 mr-1 inline-block bg-neutral-500 rounded-full list-none;
        &.active {
            @apply bg-secondary;
        }
        &.passed {
            @apply bg-secondary/25;
        }
    }
}
